структура моего сайта
---------------
About me ( мб это то, что на главной изобразил)
Образование
Карьера (места работы)
Способности, навыки (те технологии, что знаю, с оценками 1-5 мб иначе)
Проекты
Идеальная вакансия
контакты
Семейное, жилищное положения. 

====================

Проекты
------------
на Java прогу для HMM

--------------- На портфолио
"вернуться на главную" оформить кнопкой, дать маржин топ 16
выбор темы (тёмная\светлая)

6) оптимизация
-- - - -- - 
сергей кати, [10.10.2023 16:46]
я даю вам макет
1) развернули фигму
что не понятно сразу гуглите
а не идёте скачивать курс по фигме
2) делаете репозиторий
ветки
потом PR
- - - - - -
что есть PR выяснить
- - - - -
prefers-color-scheme -выяснить как-то даёт ли броузер эту переменную
const prefersDarkScheme = window.matchMedia("(prefers-color-scheme: dark)");
console.log( XXXXXXXXXXXXX);
if (prefersDarkScheme.matches) {
  document.body.classList.add("dark-theme");
} else {
  document.body.classList.remove("dark-theme");
}
= = = == = = 2й вариант
const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');
if (prefersDarkScheme.matches) {
  document.body.classList.add('dark-theme');
} else {
  document.body.classList.remove('dark-theme');
}
- - -- - - 3й вариант
/ Выбираем кнопку
const btn = document.querySelector('.btn-toggle');
const prefersDarkScheme = window.matchMedia("(prefers-color-scheme: dark)");
// Отслеживаем щелчок по кнопке
btn.addEventListener("click", function() {
  // Если ОС настроена в тёмном режиме…
  if (prefersDarkScheme.matches) {
    // …тогда применяем класс .light-theme для переопределения этих стилей
    document.body.classList.toggle("light-theme");
  // В противном случае…
  } else {
    // …применяем класс .dark-theme для переопределения светлого стиля по умолчанию
    document.body.classList.toggle("dark-theme");
  }
});


- - - - - - -  4й вар с localStorage
// Выбираем кнопку
const btn = document.querySelector(".btn-toggle");
// Выбираем настройки темы из localStorage
const currentTheme = localStorage.getItem("theme");
// Если текущая тема в localStorage равна "dark"…
if (currentTheme == "dark") {
  // …тогда мы используем класс .dark-theme
  document.body.classList.add("dark-theme");
}

// Отслеживаем щелчок по кнопке
btn.addEventListener("click", function () {
  // Переключаем класс .dark-theme при каждом щелчке
  document.body.classList.toggle("dark-theme");
  // Допустим, тема светлая
  let theme = "light";
  // Если <body> содержит класс .dark-theme…
  if (document.body.classList.contains("dark-theme")) {
    // …тогда делаем тему тёмной
    theme = "dark";
  }
  // После чего сохраняем выбор в localStorage
  localStorage.setItem("theme", theme);
});

- - - - -
===========


